#include "lists.h"
/**
 * revers_listint - reverses a listint_tlist.
 * @head: a pointer to the address of the head of the list_t list.
 *
 * Return: a pointer to the first node of the reversed list.
 */
